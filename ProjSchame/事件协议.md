# 事件协议

### 协议

每个插件需包含 `On(event, arg)` 函数，接受 Shell 广播事件

### 运行前

`pre_shell_start` Shell启动

`pre_user_wrong_password` 密码输入错误

`pre_user_login` 成功登入

### 运行中

`running_new_line` 用户输入命令

`running_state_change` 状态改变

